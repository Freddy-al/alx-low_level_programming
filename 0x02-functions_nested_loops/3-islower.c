#include "main.h"
/**
 * main - Using islower to write a program that prints if lowercase
 *
 * Return: Always 0
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1)
	}
	else
	{
		return (0);
	}
	_putcher('\n')
}
